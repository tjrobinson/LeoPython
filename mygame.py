from tkinter import *
window = Tk()
button = Button(window, text="A", width=5)
button.pack(padx=5, pady=5)
def mouse(ON):
    mouse.hover=True
def on_click(event):
    if mouse(ON) button:
        if True:
            print("A")